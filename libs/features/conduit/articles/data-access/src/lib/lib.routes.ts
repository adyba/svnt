import { Route } from '@angular/router';
import { DataAccessComponent } from './data-access/data-access.component';

export const dataAccessRoutes: Route[] = [
  { path: '', component: DataAccessComponent },
];
